({

    doInit : function (cmp, event, helper) {
        helper.doInitHelper(cmp);
    },

	handleTabNavigation : function (cmp, event, helper) {
	    helper.handleTabNavigationHelper(cmp, event);
	},

})