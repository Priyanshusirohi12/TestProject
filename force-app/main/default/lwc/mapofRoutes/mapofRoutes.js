import { LightningElement, wire, api } from 'lwc';
//import sendObjectToTargetOrg from '@salesforce/apex/RouteStationCalloutClass.sendObjectToTargetOrg';

export default class MapofRoutes extends LightningElement {
    firstValue;
    lastValue;


   /* distanceFromCar(){
        sendObjectToTargetOrg({inputValue : this.firstValue})
        .then(result =>{
            console.log(JSON.stringify(result));
        }).catch(error => {
            console.log('error');
        })
    }*/
}